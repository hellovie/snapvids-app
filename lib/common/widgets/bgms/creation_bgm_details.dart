import 'package:flutter/material.dart';

class CreationBgmDetails extends StatelessWidget {
  const CreationBgmDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Placeholder(),
    );
  }
}
